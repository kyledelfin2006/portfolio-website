---
interface Props { sections: { id: string; label: string }[]; }
const { sections } = Astro.props;
---
<nav class="section-navigator no-print" aria-label="Resume sections">
  <ol>
    {sections.map(({ id, label }) => <li><a href={`#${id}`}>{label}</a></li>)}
  </ol>
</nav>
